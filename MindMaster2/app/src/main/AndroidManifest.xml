<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.hoccob.mindmaster">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.HighScoreActivity">


        </activity>
        <activity android:name=".activity.SinglePlayerActivity">
        </activity>


    <activity android:name=".activity.HighScoreActivity2">
    </activity>

        <activity android:name=".activity.PracticeActivity">

        </activity>
        <activity android:name=".activity.MultiPlayerActivity"></activity>
        <activity android:name=".activity.LoadingActivity"
            android:noHistory="true">
        </activity>
    </application>
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
</manifest>